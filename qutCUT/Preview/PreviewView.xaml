<UserControl
    x:Class="qutCUT.Preview.PreviewView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Background="#FF0A0A0C">
        <!-- Video surface — MediaPlayerElement for realtime playback -->
        <MediaPlayerElement x:Name="Player"
                            AreTransportControlsEnabled="False"
                            Stretch="Uniform"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"/>

        <!-- Overlay: transform handles (shown when clip selected) -->
        <Canvas x:Name="TransformOverlay" IsHitTestVisible="False"/>

        <!-- Loading indicator during composition build -->
        <ProgressRing x:Name="BuildingRing"
                      IsActive="{x:Bind IsBuildingComposition, Mode=OneWay}"
                      Width="40" Height="40"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"/>
    </Grid>
</UserControl>
